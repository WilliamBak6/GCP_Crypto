
version: 2

models:
  - name: int_crypto_data
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
    config:
      materialized: view
